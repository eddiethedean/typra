[package]
name = "typra-core"
description = "Core engine for Typra, a typed embedded database."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"

[dependencies]
crc32c = "0.6"
regex = "1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
